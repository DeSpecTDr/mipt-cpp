project('week1', 'cpp',
  version : '0.1',
  default_options : [
    'warning_level=3',
    'buildtype=debugoptimized',
    'cpp_std=c++20'
  ])

exe = executable('week1', 'week1.cpp',
  install : true)

test('basic', exe)
